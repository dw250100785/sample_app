<%  provide :title, 'Home' %>
<h1>Home</h1>

<% if signed_in? %>

  <%= render 'shared/sidebar' %>
  <%= render 'shared/collection', object: @microposts %>

<% else %>

  <%= link_to 'Sign up', signup_path %>

<% end %>